# This project was generated using a Cookiecutter template:
#       https://github.com/notsatan/go-template

cookiecutter_inputs:
  project_name: "{{ cookiecutter.project_name.strip() }}"
  go_module_path: "{{ cookiecutter.go_module_path.strip('/') }}"
  license_owner: "{{ cookiecutter.license_owner }}"
  base_branch: "{{ cookiecutter.base_branch }}"
  contact_email: "{{ cookiecutter.contact_email }}"
  project_description: "{{ cookiecutter.project_description }}"
  github_specific_features: "{{ cookiecutter.github_specific_features }}"
  use_codecov: "{{ cookiecutter.use_codecov }}"
  use_precommit: "{{ cookiecutter.use_precommit }}"
  go_version: "{{ cookiecutter.go_version }}"
  license: "{{ cookiecutter.license }}"
